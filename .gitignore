# Dependencies
node_modules/

# Build output / packaged app (huge, regenerated by `npm run dist`)
release/
dist/

# Local door PIN — personal, never commit it (see remote/.pin.example)
remote/.pin

# Local env overrides
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
